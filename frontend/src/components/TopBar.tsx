import React from "react";

const TopBar: React.FC = (props) => (
    <div className="flex px-4 py-3 bg-teal-500 text-white">
        <span className="px-2 font-bold select-none bg-white text-teal-500 rounded-full">Dataviz</span>
    </div>
);

export default TopBar;